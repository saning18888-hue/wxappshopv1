# B2C 微信小程序商城 PRD

> 本文件合并原 `PRD_B2C微信小程序商城_V1.0.md` 与 `PRD_B2C微信小程序商城_V2.0_设计驱动版.md`。
> - **第一部分（V1.0）**：完整功能需求（产品目标、角色、范围、小程序 / 后台需求、DIY 系统、技术部署、数据表、验收、实施顺序、暂不纳入范围）。
> - **第二部分（V2.0 设计驱动版）**：在 V1.0 业务功能基础上以设计系统重写，本章仅收录 V2.0 新增的**设计系统**内容（Design Token、组件规范、页面级设计规范、后台 design-led、DIY token 化、视觉验收）。功能需求一律见第一部分。
>
> 设计规范同时见根目录 `UI设计规范.md`。

---

# 第一部分：功能需求（V1.0）

**版本**：V1.0
**产品形态**：微信小程序 + 自有服务器后台 + 管理端
**部署环境**：Nginx 1.22.1、PHP 8.2.31、MySQL 5.7.40、phpMyAdmin 5.2
**部署原则**：不使用微信云函数、云托管或云数据库；所有业务由自有服务器提供 API。

## 1. 产品目标

建设一套可持续扩展的 B2C/O2O 商城，支持商品销售、微信支付、快递配送、同城配送、线下自提、会员运营和后台 DIY 装修。

核心原则：

1. 小程序只负责展示和交互，商品、价格、库存、订单、营销、页面装修均以后端数据为准。
2. 支付、退款、库存扣减、订单状态变化必须在服务端完成并记录日志。
3. MVP 先跑通“浏览—购物车—下单—支付—履约—售后”闭环，再增加复杂营销。
4. DIY 页面使用后端保存的 JSON 配置渲染，尽量做到不改代码即可调整首页和商品展示布局。

## 2. 用户角色

| 角色 | 权限范围 |
|---|---|
| 游客 | 浏览首页、分类、商品、文章；可搜索、分享、授权登录 |
| 微信用户/会员 | 下单、支付、收藏、优惠券、积分、评价、售后、查看订单 |
| 店员 | 查看订单、备货、发货、核销、自提交付 |
| 运营人员 | 商品、内容、页面装修、优惠券、活动、订单处理 |
| 财务人员 | 支付、退款、对账、资金数据查看 |
| 超级管理员 | 系统配置、角色权限、操作日志、全部数据 |

## 3. 产品范围与版本规划

### 3.1 MVP 必须实现

- 微信小程序登录与会员基础资料
- 首页、分类、搜索、商品列表、商品详情
- 商品分类、SPU/SKU、规格、图片、上下架、库存、限购
- 购物车与立即购买
- 微信支付下单、支付回调、订单查询、取消、关闭
- 配送方式：快递、线下自提；同城配送预留并可单独开关
- 收货地址、运费模板、门店/自提点
- 订单列表、订单详情、发货、物流单号、确认收货
- 退款/售后申请与后台审核
- 优惠券：满减券/无门槛券，领取、使用、核销
- 会员积分基础能力与商品评价
- 首页 DIY：轮播、Banner、金刚区、商品组、分类入口、文章入口、底部菜单
- 后台登录、角色权限、操作日志、基础数据看板
- 图片/视频上传、媒体资源管理、系统参数配置

### 3.2 V1.1 优先扩展

- 同城配送、配送区域和配送费规则
- 秒杀、拼团、团购
- 电子卡券、到店核销、核销优惠券
- 文章分类、文章详情、视频内容、专题页
- 会员等级、会员价、积分抵扣、签到
- 订单打印、打印机配置、自动/手动重打
- 快递鸟或其他物流查询接口
- 分销一级返佣
- 数据报表、商品分析、交易分析、访客统计

### 3.3 V2.0 可选扩展

- 砍价、裂变分销、多级分红
- 储值余额、充值、会员卡
- 多门店、门店库存、调拨与店员端
- 批发价、阶梯价、采购/供应商
- 多端小程序适配
- 可视化营销编排、自动化消息和 CRM

## 4. 小程序端需求

### 4.1 页面结构

底部菜单建议固定为：**首页、分类、购物车、订单/会员**。页面路径统一由后台维护，避免前端写死运营入口。

主要页面：

- 首页：DIY 组件渲染
- 分类页：一级/二级分类、商品列表、筛选排序
- 搜索页：关键词、历史记录、热搜词、搜索结果
- 商品详情：轮播图、视频、价格、库存、规格、图文详情、评价、优惠券、配送说明
- 购物车：SKU 数量、失效商品、优惠估算、批量选择
- 确认订单：地址/自提点、配送方式、优惠券、积分、买家留言、价格明细
- 支付结果：成功/失败/待支付状态及订单入口
- 订单中心：全部、待付款、待发货、待收货、待评价、售后
- 会员中心：头像昵称、优惠券、积分、收藏、足迹、地址、售后
- 文章/专题/活动页
- 自提核销或电子券展示页

### 4.2 关键交互规则

- 商品价格、库存、优惠计算以前端展示为参考，提交订单时必须由服务端重新计算。
- SKU 未选择完整时禁止加入购物车或下单。
- 库存不足、活动结束、优惠券失效时，提交订单应返回明确原因。
- 支付超时订单自动关闭并释放库存；支付回调与主动查询均需幂等。
- 快递、自提、同城配送可分别在系统设置中启用；商品可限制可用配送方式。
- 自提订单生成取货码；店员核销成功后订单进入已完成或待评价状态。

## 5. 管理后台需求

### 5.1 概况

支持今日、本周、本月和自定义时间：支付金额、支付订单、待付款、待发货、售后中、会员数、商品数；提供销售趋势和待办提醒。

### 5.2 商品中心

- 商品：SPU、SKU、规格、属性、品牌、商品图、视频、图文详情、分享信息
- 价格：销售价、划线价、会员价预留、成本价仅管理员可见
- 库存：SKU 库存、锁定库存、可售库存、库存流水、低库存提醒
- 分类：多级分类、排序、图标、是否显示
- 配送：商品支持的配送方式、运费模板、重量/件数计费
- 商品状态：草稿、上架、下架、售罄、删除（软删除）

### 5.3 订单中心

订单状态建议：待付款 → 已付款/待履约 → 配货中 → 已发货/配送中/待自提 → 已完成；并行支持已取消、退款中、已退款、售后处理中。

必须记录订单快照：商品名称、规格、图片、单价、优惠、运费、实付金额，防止商品修改影响历史订单。

后台能力：筛选、批量发货、拆单预留、备注、导出、退款审核、售后凭证、物流信息、自提核销、订单打印。

### 5.4 配送与门店

- 快递：运费模板、包邮条件、指定地区、首件/续件规则
- 同城配送：开关、配送区域、起送金额、配送费、营业时间、预计时效
- 线下自提：门店名称、地址、电话、营业时间、自提说明、取货码
- 商品级限制：仅快递、仅自提、快递+自提等

### 5.5 会员与营销

MVP 包含会员资料、标签、积分流水、优惠券和基础等级字段。优惠券要配置名称、类型、面额/折扣、门槛、适用商品/分类、有效期、发放数量、每人限领、是否可叠加。

营销计算必须采用统一优惠引擎，明确叠加优先级：商品活动价 → 店铺优惠 → 优惠券 → 积分抵扣。具体规则由后台配置并写入订单优惠快照。

### 5.6 内容中心

- 文章分类、文章、专题页、图片/视频资源
- Banner、公告、外部 H5 链接、小程序跳转入口
- 内容上下架、定时发布、浏览量、分享标题和封面
- 所有跳转目标使用安全的白名单类型：商品、分类、活动、文章、订单、会员中心、外部 HTTPS 页面

## 6. DIY 装修系统

### 6.1 设计目标

运营人员可在后台选择页面、拖拽组件、修改内容和样式，保存草稿、预览、发布、回滚；发布后小程序通过接口获取页面配置。

### 6.2 MVP 组件

轮播图、单图 Banner、视频、公告、搜索框、金刚区、图标导航、分类导航、商品列表、商品横滑、商品瀑布流、优惠券、文章列表、活动入口、富文本、空白间距、底部菜单。

### 6.3 配置模型

每个页面保存：页面标识、版本号、状态、组件顺序、组件参数、发布时间、发布人。组件参数包括数据来源、跳转目标、颜色、间距、圆角、列数、显示数量和排序方式。

建议数据结构：

```json
{
  "page": "home",
  "version": 3,
  "components": [
    {"type": "banner", "sort": 1, "props": {"items": [], "interval": 4}},
    {"type": "nav_grid", "sort": 2, "props": {"columns": 5, "items": []}},
    {"type": "goods_group", "sort": 3, "props": {"source": "category", "category_id": 1}}
  ]
}
```

前端只实现组件渲染器和参数校验；后台控制组件是否可用、组件配置和发布版本。必须保留历史版本，支持一键回滚。

## 7. 技术与部署要求

### 7.1 推荐架构

小程序端 → HTTPS REST API → PHP 8.2 业务服务 → MySQL 5.7。图片和视频存储在服务器文件目录或兼容 S3 的对象存储；Nginx 负责 HTTPS、静态资源和 PHP 请求转发。

后台可采用 PHP 主流 MVC 框架（如 Laravel/ThinkPHP，最终以开发阶段确认的团队技术栈为准），前端小程序使用原生小程序或成熟跨端框架，但不得依赖云开发运行时。

### 7.2 API 基础约束

- `/api/v1` 版本化；统一 JSON 响应、错误码、分页和时间格式
- 登录态使用服务端会话或短期 Token；后台与小程序鉴权隔离
- 权限采用 RBAC；每个写操作记录操作者、IP、请求摘要和结果
- API 具备参数校验、限流、幂等键、事务和审计日志
- 生产环境禁止返回 AppSecret、支付密钥、数据库密码和堆栈信息

### 7.3 微信能力

- AppId/AppSecret 仅保存在服务端环境变量或受保护配置中，不进入小程序包
- 微信登录由服务端换取用户身份并建立会员绑定
- 微信支付统一由服务端创建支付参数、验签处理回调、查询订单和发起退款
- 证书、商户密钥、回调地址、支付目录按微信官方后台配置，并在测试环境验证
- 订阅消息、客服消息、物流能力按实际资质和微信平台审核结果接入

### 7.4 安全与合规

HTTPS、密码哈希、敏感字段脱敏、上传文件类型和大小校验、图片访问策略、SQL 注入防护、XSS 防护、CSRF 防护、备份恢复、删除和导出个人信息能力必须纳入验收。

## 8. 核心数据表规划

用户与权限：`users`、`user_profiles`、`user_addresses`、`admin_users`、`roles`、`permissions`、`admin_logs`
商品：`goods`、`goods_skus`、`goods_specs`、`goods_spec_values`、`categories`、`goods_media`、`inventory_logs`
交易：`carts`、`orders`、`order_items`、`order_payments`、`order_logs`、`refunds`、`after_sales`、`shipments`
营销：`coupons`、`coupon_templates`、`coupon_usages`、`points_logs`、`activities`、`activity_items`
内容装修：`pages`、`page_versions`、`page_components`、`articles`、`banners`、`media_files`
门店配送：`stores`、`pickup_codes`、`delivery_rules`、`shipping_templates`

所有金额使用整数分或 DECIMAL，库存操作必须有流水；订单、支付回调、退款回调不得依赖单一请求成功。

## 9. MVP 验收标准

1. 新用户可从小程序登录，浏览商品并选择 SKU。
2. 服务端能正确计算商品、运费、优惠券、积分和实付金额。
3. 微信支付成功后，无论回调重复、延迟或用户返回，小程序和后台最终状态一致。
4. 快递订单可发货并保存物流单号；自提订单可生成取货码并完成核销。
5. 用户可取消未支付订单、申请售后；后台可审核并形成退款记录。
6. 运营人员可不改代码配置首页组件、跳转、商品组和底部菜单，并可预览、发布、回滚。
7. 未授权管理员不能访问后台数据；关键操作有日志；数据库可备份并恢复。
8. 在目标服务器完成部署，使用正式域名 HTTPS，通过微信开发者平台完成真机调试和主要流程测试。

## 10. 建议实施顺序

第一阶段：服务器基础、登录、商品、分类、购物车、订单、微信支付。
第二阶段：快递/自提、库存、售后、优惠券、会员、评价。
第三阶段：DIY 装修、内容、消息通知、订单打印、数据看板。
第四阶段：秒杀、拼团、同城配送、核销、分销及其他插件化营销。

## 11. 首期暂不纳入范围

多平台小程序、复杂多级分红、储值资金体系、供应链采购、独立 App、云函数/云托管、未经确认的第三方支付或物流自动下单。它们会显著增加资金、合规、库存和售后复杂度，应在 MVP 稳定后再评估。

---

# 第二部分：设计驱动版（V2.0 设计系统）

**版本**：V2.0（设计驱动重写）
**基于**：第一部分（V1.0）功能范围 + 现有小程序结构（首页/分类/商品列表/详情/购物车/确认订单/支付结果/会员中心）
**设计语言**：Airbnb 风格（温暖 · 图片优先 · 圆角卡片 · 单一强调色 · 杂志式留白），按微信小程序 `rpx` 体系落地
**部署原则**：小程序 + 自有服务器后台；不依赖云开发运行时（同 V1.0）

> 说明：本版在保留 V1.0 全部业务功能（SPU/SKU、微信支付、快递/自提、优惠券、会员、DIY 装修、后台 RBAC）的前提下，**以设计系统为骨架重写**。所有视觉决策均来自统一 Design Token，确保"不改代码换肤"与"多端一致"。如需改为 Apple 极简风或 Spotify 暗色风，仅需替换第 1 章 Design Token 与组件库，业务章节不变（功能需求一律见第一部分）。

## 0. 设计总纲（Design Direction）

### 0.1 为什么选 Airbnb 风格
| 维度 | 现版（V1.0） | 设计驱动版（V2.0） |
|---|---|---|
| 主色 | 橙色 `#FF6B35` 通铺 | 单一强调色（默认 Rausch Red `#FF385C`，可换品牌色），只用于 CTA |
| 背景 | 浅灰 `#f5f5f5` | 纯白页面 + 浅灰分区面，图片提供全部色彩 |
| 卡片 | 16rpx 圆角、无分层阴影 | 20–40rpx 圆角 + 三层暖光阴影，触感化 |
| 字体 | 系统字体、字号不统一 | 温暖中粗字重（500–700），标题负字距，字号层级收敛 |
| 图片 | 辅助元素 | **英雄内容**：列表卡图占 60%+，详情大图优先 |
| 心智 | 货架式罗列 | 杂志式"逛"——慢滚动、沉浸、想拥有 |

### 0.2 设计北极星（North Star）
> **浏览 → 想要 → 下单**。每一个像素服务于"让商品显得值得拥有"，界面本身退到幕后。

### 0.3 验收级设计红线（贯穿全文）
1. 强调色只用于主 CTA 与品牌瞬间，禁止大面积铺底。
2. 正文文字永远用暖近黑 `#222222`，禁止纯黑 `#000000`。
3. 所有价格/强调文案用中粗字重（≥500），标题不出现细字（<500）。
4. 卡片必须三层阴影 + 大圆角，禁止锐利直角（0–4px）。
5. 商品图是主角：列表/详情/首页组件均以图优先。

## 1. 设计系统（Design System）

> 单位换算：`750rpx = 375pt`（设计基准宽）。`1px ≈ 2rpx`。以下 token 同时给出 px（设计稿）与 rpx（小程序）。

### 1.1 颜色 Token

```css
/* ===== 品牌 / Brand ===== */
--brand:          #FF385C;   /* Rausch Red，主 CTA、激活态、品牌瞬间（可替换为品牌色） */
--brand-press:    #E00B41;   /* 按下/深 variant */
--brand-tint:     rgba(255,56,92,.08);  /* 浅色填充背景（选中/聚焦） */

/* ===== 文字 / Text ===== */
--text:           #222222;   /* 主文字：暖近黑，非纯黑 */
--text-focus:     #3F3F3F;   /* 聚焦态文字 */
--text-secondary: #6A6A6A;   /* 次要文字、描述 */
--text-disabled:  rgba(0,0,0,.24);
--text-link:      #428BFF;   /* 法律/信息蓝链接 */

/* ===== 面 / Surface ===== */
--bg:             #FFFFFF;   /* 页面背景：纯白 */
--surface:        #F7F7F8;   /* 分区浅灰面（替代原 #f5f5f5，带极轻蓝灰防刺眼） */
--surface-2:      #F2F2F2;   /* 次级面：圆形按钮、输入底 */
--line:           #EBEBEB;   /* 分割线（替代原 #eeeeee，更柔） */
--border-card:    rgba(0,0,0,.02);  /* 卡片 1px 描边层 */

/* ===== 状态 / Status ===== */
--success:        #00B86B;   /* 保留原绿，用于"已发货/成功"等正向状态 */
--error:          #C13515;   /* 错误文字 */
--warning:        #FFB400;   /* 库存预警、活动角标 */

/* ===== 阴影 / Elevation（三层暖光）===== */
--shadow-card:  rgba(0,0,0,.02) 0 0 0 1rpx, rgba(0,0,0,.04) 0 4rpx 12rpx, rgba(0,0,0,.10) 0 8rpx 16rpx;
--shadow-hover: rgba(0,0,0,.08) 0 8rpx 24rpx;
```

### 1.2 字体与字号层级（Typography）

- **字体族**：`-apple-system, "PingFang SC", "Helvetica Neue", "Microsoft YaHei", sans-serif`（小程序无自定义字体，用系统中文+西文回退；如需品牌感后续可接入可变字体）。
- **字重原则**：标题/价格/CTA 一律 500–700；正文 400；**禁止**标题用 300/400 细字。

| 角色 | 尺寸(rpx) | 字重 | 行高 | 字距 | 用途 |
|---|---|---|---|---|---|
| 页面大标题 | 56 | 700 | 1.43 | -0.88rpx | 首页区块标题、详情商品名 |
| 区块标题 | 44 | 600 | 1.18 | -0.88rpx | 金刚区/商品组标题 |
| 卡片标题 | 40 | 600 | 1.20 | -0.36rpx | 商品名、列表卡 |
| 强调文本 | 32 | 500 | 1.25 | 0 | 导航、按钮、Tab |
| 正文 / 链接 | 28 | 400 | 1.43 | 0 | 标准正文（=原 28rpx 基准） |
| 次要描述 | 26 | 400 | 1.23 | 0 | 副标题、说明 |
| 标签/价格 | 24 | 400–700 | 1.33 | 0 | Tag、角标、价格 |
| 微标 | 22 | 600 | 1.18 | 0 | 状态徽章（uppercase 场景） |

> **统一字号约定（呼应后台修复）**：全站只用 56/44/40/32/28/26/24/22 八档；弹窗与表单主文 28rpx、次文 26rpx，杜绝 12px 以下的"看不清"字号。

### 1.3 间距与栅格（Spacing & Grid）

- **基准单位**：`16rpx`（=8px）。尺度：`8 / 12 / 16 / 22 / 24 / 32 / 44 / 48rpx`。
- **页面边距**：`24rpx`（内容容器左右留白）。
- **卡片间距**：纵向 `24rpx`，横向 `16rpx`。
- **栅格**：商品列表 2 列（默认）、单行大卡 1 列（详情推荐/专题）；首页组件列数由 DIY 配置（2/3/4/5）。

### 1.4 圆角尺度（Radius）

| 等级 | rpx | 用途 |
|---|---|---|
| 微 | 8 | 小标签、链接 |
| 标准 | 16 | 按钮、Tab、搜索框 |
| 徽章 | 28 | 状态徽章、角标 |
| 卡片 | 40 | 商品卡、大按钮 |
| 大容器 | 64 | Hero、弹窗、底部菜单容器 |
| 圆 | 50% | 圆形按钮、头像、导航箭头 |

### 1.5 组件规范（Component Specs）

**按钮**
- 主 CTA：底 `--brand`，白字，圆角 999rpx（胶囊），高 88rpx，字号 32rpx 字重 500，左右 padding 48rpx；按下 `--brand-press`，禁用 `#FFC4AD`。
- 次按钮：底 `--surface-2`（`#f2f2f2`），文字 `#222`，圆角 999rpx。
- 圆导航按钮：底 `#f2f2f2`，圆 50%，hover 阴影 + 位移。

**卡片（商品卡为标准件）**
- 白底，圆角 40rpx，三层 `--shadow-card`。
- 图片占卡上部（比例 1:1 或 4:3，DIY 可配），下方：商品名 28–32rpx/500–600 `#222` → 描述 24rpx/400 `#6a6a6a` → 价格行（品牌色，中粗）→ 角标。
- 收藏心形：透明底、圆 50%、白心 + 暗描边，叠在图右下。

**标签 / 徽章**
- 状态徽章：圆角 28rpx，`--success`/`--warning`/`--brand-tint` 浅底 + 对应深字。
- 活动角标（秒杀/券）：品牌色底白字或白底品牌色边。

**输入 / 搜索**
- 搜索条：白底、三层阴影、圆角 64rpx，占位 26rpx `#999`，聚焦 `--brand-tint` 浅底 + 2rpx 聚焦环。
- 表单输入：底 `#f2f2f2`，圆角 16rpx，字号 28rpx，聚焦环 `--brand`。

**图片处理**
- 商品图优先；轮播点指示；大图比例由组件配置。
- 列表图默认 1:1，详情主图 4:3 或 1:1，视频支持 16:9。
- 懒加载、按比例占位（浅灰骨架），防跳动。

### 1.6 Do & Don't（设计红线速查）

- ✅ 用 `#222` 暖近黑；✅ 强调色只给 CTA；✅ 标题中粗字重；✅ 三层阴影 + 大圆角；✅ 图片做主角；✅ 标题负字距。
- ❌ 纯黑文字；❌ 强调色铺大面；❌ 标题细字（<500）；❌ 重阴影（>0.1）；❌ 卡片锐角（<16rpx）；❌ 引入额外品牌色；❌ 覆盖 token 直接写死色值。

## 2. 产品目标与原则（精简自第一部分）

建设可持续扩展的 B2C/O2O 商城，跑通"浏览—购物车—下单—支付—履约—售后"闭环。**设计原则**：小程序只负责展示与交互，价格/库存/优惠以服务端为准；营销与页面装修由后端 JSON 配置驱动，尽量不改代码调布局。功能角色与范围见第一部分第 1–3 章。

## 3. 信息架构与导航（Design-led IA）

### 3.1 底部 TabBar（4 项，设计驱动）
| Tab | 文字 | 图标 | 选中态 |
|---|---|---|---|
| 首页 | 首页 | 屋形线性图标 | `--brand` 填充 + 轻微放大 |
| 分类 | 分类 | 网格/列表图标 | `--brand` |
| 购物车 | 购物车 | 购物袋图标 + 角标（数量） | `--brand` |
| 我的 | 我的 | 人形图标 | `--brand` |

- 未选 `#999`，选中 `#FF385C`；图标用圆角线性风格，选中态加 8rpx 圆底 `--brand-tint`。
- 背景纯白 + 顶部 1rpx `--line`；高度 ~100rpx，符合 44pt 触摸目标。

### 3.2 页面树（同第一部分，新增设计归属）
首页 → 分类 / 商品列表 → 商品详情 → 购物车 → 确认订单 → 支付结果；会员中心；文章/专题/活动；自提核销页。所有入口由后台维护，前端不写死。

## 4. 小程序端页面级设计规范

> 每页给出：布局骨架 + 视觉规则 + 关键组件 + 动效。功能逻辑沿用第一部分第 4 章。

### 4.1 首页（DIY 卡片化 · 图片优先）
- **布局**：纯白背景，纵向流式；顶部胶囊搜索（圆角 64rpx，吸顶半透）；组件间 24rpx 留白，营造"杂志慢滚"。
- **组件**：轮播（大图 4:3，圆角 40rpx，圆点指示）→ 金刚区（图标圆 50%，横向滚动，激活态底部短横）→ 商品组（2 列卡，图优先）→ 横滑商品（单卡 1 列大图）→ 优惠券/文章入口。
- **视觉**：每个组件区块用 44rpx 区块标题（负字距）；组件卡白底三层阴影；背景分区用 `#F7F7F8` 隔断。
- **动效**：卡片入场轻微上浮（8rpx→0 + 透明度）；轮播自动 4s；下拉刷新骨架屏。

### 4.2 分类页
- 左：一级分类列表（选中项左侧 6rpx `--brand` 竖条 + 白底卡片）；右：二级分类 + 商品列表（2 列卡）。
- 顶部分类入口图标圆角；筛选/排序条吸顶，圆角 16rpx 胶囊。

### 4.3 商品列表
- 2 列瀑布/网格卡；图 1:1 占主，下方商品名（28rpx/500）+ 价格（品牌色中粗）+ 角标。
- 筛选抽屉：从右滑入，圆角 64rpx 顶部，遮罩 `--text` 30% 黑；选项胶囊化。

### 4.4 商品详情（大图 · 规格卡片化）
- 顶部主图轮播（4:3，圆点，心形收藏叠图右）；视频支持 16:9。
- 价格区：品牌色大价（44rpx/700）+ 划线价（24rpx `#999` 删除线）+ 活动角标；**右侧内置「收藏」（☆/★ 切换态，激活变红）+「分享」（`<button open-type="share">` 调起转发面板）快捷按钮**，与底部固定栏收藏/加购形成双入口。
- 价格口径：服务端以「分」存储，对外输出统一转换为「元」（保留两位小数，如 `1990` → ¥19.90，含 SPU 价与 SKU 价）；前端取字段直接渲染，不做二次换算。
- **规格选择（呼应后台卡片化）**：规格组以卡片/标签呈现，选中态 `--brand` 边 + `--brand-tint` 底；**标签内文字垂直居中、高度统一（约 56rpx，纯 padding 对称撑开，规避微信 `<text>` 基线偏下）**；未选完整禁止加购（按钮置灰 `#FFC4AD`）。
- 图文详情：富文本区（字号 28rpx 行高 1.6，图片圆角 16rpx）；评价区卡片化。
- 底部固定栏：收藏 + 加入购物车 + 立即购买（双 CTA，主 `#FF385C` 胶囊，次 `#222` 胶囊）。

### 4.5 购物车
- 商品行卡：图 1:1（圆角 16rpx）+ 名/规格/单价/数量步进器；选中圆 50% 勾选。
- 失效商品灰化分组；底部结算栏吸底，合计价品牌色，结算按钮胶囊。
- 优惠估算、批量选择，沿用第一部分逻辑。

### 4.6 确认订单
- 地址卡（圆角 40rpx，右箭头）；配送方式分段控件（胶囊选中 `--brand`）；优惠券/积分行（点击展开抽屉）；价格明细（标签 24rpx 右对齐，实付品牌色大号）；买家留言输入框（底 `#f2f2f2` 圆角 16rpx）。
- 提交按钮吸底胶囊主 CTA。

### 4.7 支付结果
- 状态图标大圆（成功 `#00B86B`、失败 `#C13515`、待付 `#FFB400`）；标题 44rpx/700；订单入口按钮胶囊。

### 4.8 会员中心
- 头部：头像圆 50%（底 `--surface-2`）+ 昵称 40rpx/600 + 会员等级徽章（圆角 28rpx `--brand-tint`）。
- 资产行：优惠券/积分/收藏/足迹 以四宫格卡呈现（图标圆 50% + 数字 44rpx/700）。
- 列表项：左图标 + 文字 28rpx，右箭头；分组用 `#F7F7F8` 面 + 间距。

### 4.9 文章 / 专题 / 活动 / 自提核销
- 文章详情：大图 Hero（圆角 40rpx）+ 标题 44rpx/700 + 正文 28rpx 行高 1.6。
- 自提核销：取货码以大字距卡片呈现，扫码按钮主 CTA。

## 5. 管理后台（Design-led Admin）

> 沿用第一部分第 5 章功能；视觉落实"统一字号 + 卡片化"，呼应已修复的规格/属性 UI。

### 5.1 统一字体/字号体系（后台）
- 弹窗主文 14px、次文 13px（沿用修复成果）；正文 14px，标题 16–20px 中粗。
- 表单控件显式继承字号（input/textarea/select），杜绝 12px 以下。
- 规格与属性采用**同一套卡片式编辑器**：规格值标签化、属性卡片化，增删拖拽一致；富文本选区不丢失（mousedown 阻止默认 + selectionchange 保存）。

### 5.2 概况看板
- 指标卡：白底圆角 8px + 轻阴影；数字 20px/700；趋势用浅色面积图。待办提醒徽章。

### 5.3 商品中心 / 订单中心 / 配送门店 / 会员营销
- 列表表格：行间 12px 留白，状态用彩色徽章；批量操作工具栏吸顶。
- 订单快照、退款审核、自提核销、打印：表单与弹窗统一 14/13px 字号与圆角卡片。

## 6. DIY 装修系统（Token 驱动）

- 组件参数新增**设计 token 字段**：`radius`、`gap`、`columns`、`accent`、`imageRatio`、`shadow`，使运营调样式不依赖开发。
- 配置模型（扩展第一部分）：

```json
{
  "page": "home",
  "version": 3,
  "components": [
    {"type":"banner","sort":1,"props":{"items":[],"interval":4,"radius":40,"ratio":"4:3"}},
    {"type":"nav_grid","sort":2,"props":{"columns":5,"gap":16,"activeColor":"--brand"}},
    {"type":"goods_group","sort":3,"props":{"source":"category","category_id":1,"columns":2,"cardRadius":40}}
  ]
}
```
- 前端渲染器按 token 取值，未配置则用默认值；后台控制组件可用性、预览、发布、回滚（保留历史版本）。

## 7. 技术与部署（精简）

沿用第一部分第 7 章：小程序 → HTTPS REST API（`/api/v1`）→ PHP 业务服务 → 数据库；RBAC、审计日志、幂等、HTTPS、密钥不落端。设计层补充：
- 全局 Design Token 以 CSS 变量（小程序 `page` 作用域）与后台 CSS 变量统一管理，换肤只改 token。
- 图片/视频对象存储 + CDN，缩略图按组件比例下发，降低首屏。

## 8. 验收标准（新增视觉验收）

1–8 条同第一部分功能验收；补充：
9. **设计一致性**：全站色值仅来自第 1 章 token，无硬编码色；强调色未大面积铺底。
10. **字号可读性**：正文 ≥28rpx、标签 ≥22rpx；弹窗主/次文 14/13px 且控件继承字号。
11. **卡片规范**：商品/内容卡均为大圆角 + 三层阴影；无锐角卡片、无纯黑文字。
12. **图片优先**：首页/列表/详情商品图占比达标，图加载有骨架占位不跳动。
13. **换肤可行**：仅改 Design Token 即可整体换色/换圆角，业务代码零改动。

## 9. 实施顺序（设计先行）

1. **设计基建**：落地第 1 章 Design Token（小程序 + 后台），封装按钮/卡片/标签/输入通用组件。
2. **首页 + 商品流**：按 4.1–4.4 重做首页/分类/列表/详情（图片优先、卡片化）。
3. **交易闭环**：购物车/确认订单/支付结果（沿用逻辑 + 新组件）。
4. **会员中心 + 内容**：4.8–4.9。
5. **后台重构**：5.x 统一字号与卡片化编辑器（规格/属性/富文本）。
6. **DIY 装修 token 化**：第 6 章。

> 注：第一部分第 3 章（MVP/V1.1/V2.0 范围）、第 8 章（数据表）、第 11 章（暂不纳入范围）功能规划保持不变，本版仅重写其"呈现方式"。
